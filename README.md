# AIRLINE_PASSENGER_PREDICTION_LSTM
This project uses an LSTM model to predict future airline passenger counts based on historical time-series data. LSTM effectively captures long-term trends and seasonal patterns, enabling accurate forecasting of passenger demand.
